export enum Page {
    Home = 'home',
    Projects = 'projects',
    Activity = 'activity'
}

export enum Theme {
    Dark = 'dark',
    Light = 'light'
}

export enum GithubEventType {
    Push = 'PushEvent',
    Pull = 'PullRequestEvent',
    Create = 'CreateEvent'
}